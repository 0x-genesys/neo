# Core ML libraries
numpy<2.0  # Must be <2.0 for PyTorch compatibility
pandas==3.0.2
scikit-learn==1.8.0

# Visualization
matplotlib==3.10.8
seaborn==0.13.2

# PyTorch (for Mac - MPS support)
# Compatible with PyTorch 2.0+
torch>=2.0.0
torchvision>=0.15.0

# TPU Support (optional - for Google Cloud TPU)
# Uncomment to enable TPU training:
torch_xla>=2.0.0

# Transformers and NLP
transformers>=4.30.0
datasets>=2.14.0
tokenizers>=0.13.0
sentencepiece>=0.1.99
tiktoken>=0.5.0  # For GPT-4 tokenizer support

# Fine-Tuning with LoRA
peft>=0.5.0  # Parameter-Efficient Fine-Tuning (LoRA, QLoRA, etc.)
accelerate>=0.20.0  # Distributed training and mixed precision
bitsandbytes>=0.41.0  # Quantization support (optional, for QLoRA)
scipy>=1.10.0  # Required for some PEFT operations
safetensors>=0.3.0  # Safe tensor serialization

# Training utilities
tqdm>=4.65.0
pyyaml>=6.0
tensorboard>=2.13.0
wandb>=0.15.0

# HuggingFace Hub
huggingface-hub>=0.16.0

# Development
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0